{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug test file",
      "command": "npm test -- ${file} --run",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
